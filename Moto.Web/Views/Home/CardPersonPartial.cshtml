@using Moto.Web.Areas.Admin.ViewModel;
@model MainViewModel;

@{
    Layout = null;
}

@*<link rel="stylesheet" type="text/css" href="path/to/slick/slick.css" />
<link rel="stylesheet" type="text/css" href="path/to/slick/slick-theme.css" />
*@


    <!-- Ваши элементы слайдера -->

@if (Model.ItemCards.Elements != null && Model.ItemCards.Elements.Any())
{
    foreach (var person in Model.ItemCards.Elements)
    {
        <div class="persons-item">
            <div class="img">
                @if (person.BasePhoto64 is not null)
                {
                    <img src="@person.BasePhoto64" alt="">
                }
                <a class="ref" onclick="test(@person.Id)" style="cursor:pointer;"></a>
            </div>

            <div class="title">@($"{person.LastName} {person.FirstName}")</div>

            <a class="fancy more" onclick="test(@person.Id)" style="cursor:pointer;">Подробнее</a>
        </div>
    }
}



@*<script src="path/to/jquery/jquery.min.js" asp-append-version="true"></script>
<script src="path/to/slick/slick.min.js" asp-append-version="true"></script>*@
<!-- Подключение стилей Slick Carousel -->


<!-- Подключение скриптов Slick Carousel -->


<script>
    $(document).ready(function () {
        var itemsPerPage = 4; // Количество элементов на одной странице
        var currentPage = 1; // Текущая страница
        var totalItems = @Model.ItemCards.Elements.Count(); // Общее количество элементов
        var totalPages = Math.ceil(totalItems / itemsPerPage); // Общее число страниц

        // Функция для отображения элементов текущей страницы
        function showPage(page) {
            var startIndex = (page - 1) * itemsPerPage;
            var endIndex = startIndex + itemsPerPage;
            $("#person-block .persons-item").hide().slice(startIndex, endIndex).show();
        }

        // Инициализация страницы
        showPage(currentPage);
        $("#currentPage").text(currentPage);
        $("#totalPages").text(totalPages);

        // Обработчик нажатия кнопки "Назад"
        $("#prevPage").click(function () {
            if (currentPage > 1) {
                currentPage--;
                showPage(currentPage);
                $("#currentPage").text(currentPage);
            }
        });

        // Обработчик нажатия кнопки "Вперед"
        $("#nextPage").click(function () {
            if (currentPage < totalPages) {
                currentPage++;
                showPage(currentPage);
                $("#currentPage").text(currentPage);
            }
        });
    });
</script>
@*
<script>
    // Инициализация слайдера
    $(document).ready(function () {
        $('.persons-slider').slick({
            // Параметры настройки слайдера, например:
            dots: true, // Показывать точки-индикаторы
            arrows: true, // Показывать стрелки навигации
            autoplay: true, // Автоматическое прокручивание слайдов
            // Другие параметры настройки по вашему выбору
        });
    });
</script>



@*<style>
    *,
    *::before,
    *::after {
        -webkit-box-sizing: border-box;
        box-sizing: border-box;
    }

    body {
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol';
    }

    .container {
        max-width: 700px;
        margin: 0 auto;
    }

    .itc-slider-item {
        flex: 0 0 50%;
        max-width: 50%;
        height: 250px;
        display: flex;
        justify-content: center;
        align-items: center;
        color: rgba(255, 255, 255, 0.8);
        font-size: 7rem;
    }

        .itc-slider-item:nth-child(1) {
            background-color: #f44336;
        }

        .itc-slider-item:nth-child(2) {
            background-color: #9c27b0;
        }

        .itc-slider-item:nth-child(3) {
            background-color: #3f51b5;
        }

        .itc-slider-item:nth-child(4) {
            background-color: #03a9f4;
        }

        .itc-slider__item:nth-child(5) {
            background-color: #4caf50;
        }
</style>
<div class="container">
    <div class="itc-slider" data-slider="itc-slider" data-loop="true" data-autoplay="false">
        <div class="itc-slider-wrapper">
            <div class="itc-slider-item">
                <div class="itc-slider-item">
                    <!-- Контент 1 слайда -->
                    1
                </div>
                <div class="itc-slider-item">
                    <!-- Контент 2 слайда -->
                    2
                </div>
                <div class="itc-slider-item">
                    <!-- Контент 3 слайда -->
                    3
                </div>
                <div class="itc-slider-item">
                    <!-- Контент 4 слайда -->
                    4
                </div>
                <div class="itc-slider-item">
                    <!-- Контент 5 слайда -->
                    5
                </div>
            </div>
        </div>
        <button class="itc-slider-btn itc-slider-btn-prev"></button>
        <button class="itc-slider-btn itc-slider-btn-next"></button>
    </div>
</div>

*@